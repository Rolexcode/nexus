# Optional: enable shared incident and attestation state for the hackathon demo.
# These are Firebase Web configuration values and are safe to expose to the browser.
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
